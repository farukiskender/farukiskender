name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *" 
  workflow_dispatch:
  push:
    branches:
    - main

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: farukiskender
          outputs: |
            # Bu satır yeşil yılanı üretir
            dist/github-contribution-grid-snake.svg
            # Bu satır karanlık yılanı üretir (yedekte dursun)
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: farukiskender
          outputs: |
            # Bu, mevcut açık tema (beyaz arka plan) için
            dist/github-contribution-grid-snake.svg
            # 👇 BU SATIRI EKLE VEYA DÜZENLE 👇
            # Bu, karanlık tema (siyah arka plan, yeşil yılan) için
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          
